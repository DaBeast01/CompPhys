The code is streamlined so you can run it easily

You can switch the values of the constants in the config.hpp file

This program solves the poisson equation pde for any arbitrary 2d function
You can switch the forcing function in the main.cpp file under the functcalc() function at the bottom

to run, just type ./run.sh and it should work

:D